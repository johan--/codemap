{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "command -v codemap >/dev/null 2>&1 || pip install -q git+https://github.com/AZidan/codemap.git"
          }
        ]
      }
    ]
  }
}
